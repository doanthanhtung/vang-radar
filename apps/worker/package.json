{
  "name": "@vang-radar/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/apps/worker/src/main.js",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "@vang-radar/config": "workspace:*",
    "@vang-radar/db": "workspace:*",
    "@vang-radar/domain": "workspace:*",
    "@vang-radar/logger": "workspace:*",
    "bullmq": "^5.34.1",
    "ioredis": "^5.4.2",
    "nodemailer": "^9.0.1",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
